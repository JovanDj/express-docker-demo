services:
  #   web:
  #     build:
  #       context: ./
  #       target: dev
  #     volumes:
  #       - .:/src
  #     command: npm run start:dev
  #     ports:
  #       - "3000:3000"
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    env_file:
      - .env
